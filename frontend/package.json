{
  "name": "frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run env:dev && nuxt dev",
    "build:dev": "npm run env:dev && nuxt build",
    "preview:dev": "npm run env:dev && nuxt preview",
    "build:prod": "npm run env:prod && nuxt build",
    "preview:prod": "npm run env:prod && nuxt preview",
    "env:dev": "copy .env.development .env",
    "env:prod": "copy .env.production .env",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8",
    "nuxt": "^4.4.2"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "msw": "^2.12.14"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
